#!/usr/bin/env bash
if [ -z "$1" ]; then
  export PATH=${0%/*/*}/libexec:$PATH
  eink-help
elif [[ `file --mime-type $1 | grep "image/"` ]]; then
  export PATH=${0%/*/*}/libexec:$PATH
  eink-render -i $1
  eink-draw
else
  PATH=${0%/*/*}/libexec:$PATH "${0##*/}-${1-help}" "${@:2}"
fi
